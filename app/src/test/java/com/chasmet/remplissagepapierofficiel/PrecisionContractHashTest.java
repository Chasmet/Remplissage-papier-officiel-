package com.chasmet.remplissagepapierofficiel;
import org.junit.Test;
import static org.junit.Assert.*;
public class PrecisionContractHashTest { @Test public void containsNoSnap(){assertTrue(PrecisionContractHash.value().contains("no-snap"));} }
